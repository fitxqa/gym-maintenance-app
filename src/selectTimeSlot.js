function selectTimeSlot(event) {
    // Ensure event is defined
    if (!event) {
        console.error('Event parameter is missing');
        return;
    }

    // Example: getting the time slot from event
    const timeSlot = event.target.value;

    // Code logic to handle time slot selection\n    console.log('Selected time slot:', timeSlot);
    // Add further handling logic as needed
}